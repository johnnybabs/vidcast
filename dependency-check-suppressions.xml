<?xml version="1.0" encoding="UTF-8"?>
<!--
  OWASP Dependency-Check suppression file for VidCast.
  Add entries here only for confirmed false positives — findings where
  the CVE does not apply to VidCast's usage of the package.

  Format:
    <suppress>
      <notes>Justification for suppression</notes>
      <packageUrl regex="true">pkg:pypi/PACKAGE@VERSION</packageUrl>
      <cve>CVE-YYYY-NNNNN</cve>
    </suppress>

  Every suppression must document WHY it is not a real risk for VidCast.
  Suppressions expire automatically when the package version changes.
-->
<suppressions xmlns="https://jeremylong.github.io/DependencyCheck/dependency-suppression.1.3.xsd">

  <!-- Add service-specific false positives here as they are identified on first run -->

</suppressions>
